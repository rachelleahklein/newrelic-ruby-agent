name: Test

on: 
  pull_request: 

jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set environment variable
      run: |
        echo ::set-env name=FOO::'bar'
    - name: Use environment variable in bash script
      run: echo ::set-env name=BAZ::$(ruby ./.github/scripts/otp.rb)
    - name: Check values
      run: |
          echo ${{ env.FOO}}
          echo ${{ env.BAZ }}
